# WebSocket Client

Современный HTML/JavaScript клиент для тестирования WebSocket соединений с поддержкой чата и уникальных имен пользователей.

## 🚀 Быстрый старт

Откройте `websocket-test.html` в браузере или перейдите по адресу:
- Локально: `http://localhost:9092`
- Продакшен: `https://qabase.ru`

## ✨ Возможности

- **Real-time чат** с поддержкой множественных клиентов
- **Уникальные имена пользователей** с проверкой дублирования
- **Автоматические предложения** альтернативных имен при конфликтах
- **Адаптивный дизайн** для мобильных устройств
- **Различные типы сообщений**: chat, ping, echo, system
- **Визуальная обратная связь** для статуса соединения
- **Автоматическое переподключение** при обрыве связи

## 🎨 Интерфейс

### Основные элементы

- **Поле имени пользователя** - введите уникальное имя
- **Кнопка подключения** - установить WebSocket соединение
- **Поле сообщения** - введите текст для отправки
- **Область чата** - отображение всех сообщений
- **Статус соединения** - индикатор состояния подключения

### Цветовая схема

- 🟢 **Зеленый** - подключено
- 🔴 **Красный** - отключено
- 🟡 **Желтый** - подключение...

## 🔧 Функциональность

### Подключение

1. Введите имя пользователя
2. Нажмите "Подключиться"
3. Дождитесь подтверждения подключения

### Отправка сообщений

1. Убедитесь, что соединение установлено
2. Введите сообщение в поле ввода
3. Нажмите Enter или кнопку "Отправить"

### Обработка ошибок

#### Дублирование имен

При попытке подключиться с уже занятым именем:
1. Появляется алерт с объяснением
2. Предлагаются альтернативные варианты имен
3. Соединение закрывается с кодом 1008

#### Потеря соединения

При обрыве связи:
1. Статус меняется на "Отключено"
2. Показывается причина закрытия
3. Можно переподключиться

## 📱 Адаптивность

### Мобильные устройства

- Скрытие шапки при скролле
- Увеличенные кнопки для касания
- Оптимизированная высота области чата
- Контекстное меню для тестовых сценариев

### Планшеты

- Адаптивная сетка элементов
- Оптимальное использование пространства
- Сохранение всех функций

## 🧪 Тестовые сценарии

### Базовое тестирование

1. **Одиночное подключение**
   - Подключитесь с уникальным именем
   - Отправьте тестовое сообщение
   - Проверьте отображение в чате

2. **Множественные подключения**
   - Откройте несколько вкладок
   - Подключитесь с разными именами
   - Отправьте сообщения между вкладками

### Тестирование дублирования имен

1. Подключитесь с именем "Тест"
2. В другой вкладке попробуйте подключиться с тем же именем
3. Проверьте появление алерта с предложениями

### Тестирование стабильности

1. **Ping/Pong тестирование**
   - Используйте кнопку "Ping"
   - Проверьте получение ответа "Pong"

2. **Echo тестирование**
   - Используйте кнопку "Echo"
   - Проверьте получение эха сообщения

## 🔧 Настройка

### Изменение URL сервера

Отредактируйте переменную `wsUrl` в JavaScript:

```javascript
// Локально
const wsUrl = `ws://localhost:9092/api/websocket?username=${encodeURIComponent(username)}`;

// Продакшен
const wsUrl = `wss://qabase.ru/api/websocket?username=${encodeURIComponent(username)}`;
```

### Изменение стилей

Основные CSS переменные:

```css
:root {
  --primary-color: #ff6b35;
  --secondary-color: #ff4757;
  --success-color: #2ecc71;
  --error-color: #e74c3c;
  --warning-color: #f39c12;
}
```

### Добавление новых типов сообщений

1. Добавьте обработку в `ws.onmessage`
2. Создайте соответствующие CSS классы
3. Обновите функцию `addMessage()`

## 🐛 Отладка

### Проблемы с подключением

1. **Проверьте консоль браузера** (F12)
2. **Убедитесь, что сервер запущен**
3. **Проверьте URL WebSocket**

### Проблемы с отображением

1. **Очистите кэш браузера** (Ctrl+F5)
2. **Проверьте CSS загрузку**
3. **Убедитесь в поддержке WebSocket**

### Проблемы с именами пользователей

1. **Проверьте валидацию на сервере**
2. **Убедитесь в уникальности имени**
3. **Проверьте логи сервера**

## 📊 Мониторинг

### Консоль браузера

Клиент выводит подробные логи:
- Подключение/отключение
- Отправленные/полученные сообщения
- Ошибки соединения

### Сетевые запросы

В DevTools → Network можно отследить:
- WebSocket соединения
- HTTP запросы к серверу
- Статус коды ответов

## 🔒 Безопасность

### Валидация на клиенте

- Проверка пустых имен пользователей
- Ограничение длины сообщений
- Санитизация HTML в сообщениях

### CORS защита

Клиент работает только с разрешенными доменами:
- `localhost:9092` (разработка)
- `qabase.ru` (продакшен)

## 📝 API

### WebSocket сообщения

#### Отправка сообщения
```javascript
const message = {
    type: 'chat',
    content: 'Текст сообщения'
};
ws.send(JSON.stringify(message));
```

#### Получение сообщения
```javascript
ws.onmessage = function(event) {
    const message = JSON.parse(event.data);
    // Обработка сообщения
};
```

### События WebSocket

```javascript
// Подключение
ws.onopen = function(event) {
    console.log('Подключено');
};

// Получение сообщения
ws.onmessage = function(event) {
    console.log('Получено:', event.data);
};

// Закрытие соединения
ws.onclose = function(event) {
    console.log('Закрыто:', event.code, event.reason);
};

// Ошибка
ws.onerror = function(error) {
    console.error('Ошибка:', error);
};
```

## 🚀 Развертывание

### Статические файлы

Клиент состоит из одного HTML файла, который можно:
- Разместить на любом веб-сервере
- Открыть локально в браузере
- Встроить в существующий сайт

### Интеграция с сервером

Сервер автоматически обслуживает клиент по адресу `/`:
- `http://localhost:9092` - локально
- `https://qabase.ru` - продакшен

## 📝 Changelog

### v1.0.0
- Базовая функциональность WebSocket клиента
- Поддержка чата с множественными клиентами
- Уникальные имена пользователей
- Адаптивный дизайн
- Обработка ошибок дублирования имен
- Автоматические предложения альтернативных имен
